const usersRepository = require('./users.repository');
const productsRepository = require('./products.repository');
const ordersRepository = require('./order.repository');

module.exports = {
  usersRepository,
  productsRepository,
  ordersRepository,
};
